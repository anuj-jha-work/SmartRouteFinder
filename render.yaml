services:
  - type: web
    name: smart-route-finder-cpp
    runtime: node
    buildCommand: g++ main.cpp -o server -I include -std=c++17 -O3 -pthread
    startCommand: ./server
    healthCheckPath: /healthz
    plan: free
    autoDeployTrigger: commit
